# FastAPI backend URL (embedded at build time by Vite).
# Local: copy to `.env` — defaults to http://localhost:8000 when omitted in dev.
# Vercel: Project Settings → Environment Variables → VITE_API_URL=https://your-api.example.com
VITE_API_URL=http://localhost:8000
